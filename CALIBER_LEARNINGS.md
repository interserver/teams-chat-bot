# Caliber Learnings

Accumulated patterns and anti-patterns from development sessions.
Auto-managed by [caliber](https://github.com/caliber-ai-org/ai-setup) — do not edit manually.

- **[gotcha]** Running `npm test` or `node --test 'test/*.test.js'` via the harness's Bash background runner sometimes produces zero output even after the test runner clearly started subprocesses for each test file — the runner's `--test-isolation=process` interacts badly with the harness's task-output capture and the run wedges. For reliable output, run a single file in the foreground with an explicit timeout: `timeout 30 node --test test/<file>.test.js`. `npm test` does still work when invoked normally, just not from inside that background-task flow.
- **[pattern]** Every `saveRecent` call site that seeds a brand-new trackable (i.e. `handleSingleNew` and its `tryConstructedConvRef` fallback) MUST go through the `initialTrackableState(env)` helper, which runs `env.message` through `splitHeaderAndBullets` so the persisted `{ header, items, header_identity }` matches what `mergeGithubTrackable` would produce. **Why:** if you store the unsplit verbose `env.message` as `header` with `items: []`, the next re-delivery of the same event compares `splitHeaderAndBullets(incoming).headerText` (first line only) against the multi-line stored header, mismatches, and appends the entire event as a nested bullet → duplicated push with a sub-bullet of itself. **How to apply:** never write `header: env.message, items: []` literally — call `initialTrackableState(env)` and spread it.
- **[pattern]** The re-delivery dedup check inside `mergeGithubTrackable` compares the **first non-empty line** of the incoming `bulletMessage` against the **first non-empty line** of the stored `header`, NOT the full text on either side. This works whether the stored header is already split (header = first line, items hold commit list) or is still in legacy unsplit form from older Redis entries. Any rewrite of the re-delivery branch must preserve this two-sided first-line compare or existing legacy entries will start producing duplicate output.
- **[convention]** GitHub Actions check_run names use ` · ` (space-middledot-space, U+00B7) as the segment separator, not `/` or `-`. Two distinct naming patterns appear in practice and both must be handled by any decomposer: `<Platform> · PHP X.Y · <lib>` (e.g. `Windows · PHP 8.3 · candy-core`) and `<Phase> PHP X.Y · <lib>` where the PHP version is glued to the leading phase segment (e.g. `Test PHP 8.4 · candy-vt`). The PHP segment is matched by `/(PHP \d+\.\d+)/`; everything before it is the platform/phase, everything after is the lib.
- **[gotcha]** `test/commands.test.js` has a long-standing failing test: `ticketQuick command "does not match unknown department"` at line 135 expects `null` but `ticketQuick.match('add foo ticket bar', ...)` returns `{ invalidDept: 'foo' }`. The other tests pass and the failure pre-dates current work — don't treat it as a regression of your changes. Fix it deliberately (either tighten `ticketQuick.match` to return null for unknown depts, or update the test) rather than as a side-effect.
- **[pattern]** When debugging the notification consumer's render output, drive `mergeGithubTrackable` directly from a one-liner `node -e "..."` script that constructs the minimal envelope shape and chains multiple calls (each merging the previous `recent` with a new env). Faster than spinning up the full consumer + Redis, and matches exactly what the production code does. Example envelope: `{ type: 'msg', message: '...', extra: { event_type: 'check_run', _commit_sha: '<sha7>', dedup_key: 'github:commit:<sha7>', data: { check_run: { name, status, html_url } } } }`.
- **[convention]** GitHub Actions check_run names use ` · ` (space-middledot-space, U+00B7) as the segment separator, not `/` or `-`. Three distinct naming patterns appear in practice and `decomposeCheckSegments` in `notificationConsumer.js` must handle all of them: `<Platform> · PHP X.Y · <lib>` (e.g. `Windows · PHP 8.3 · candy-core`), `<Phase> PHP X.Y · <lib>` where the PHP version is glued to the leading phase segment (e.g. `Test PHP 8.4 · candy-vt`), and `<prefix> (<variant>)` paren style (e.g. `render (candy-vcr)`). The PHP segment is matched by `/(PHP \d+\.\d+)/`; an embedded `PHP X.Y` inside a `·`-segment is split out into its own segment so all PHP X.Y siblings collapse under one bucket.
- **[correction]** Microsoft Teams' Markdown renderer ONLY visually nests bullets one level deep — `    - foo` (4-space indent) collapses back to the same depth as `- foo` in the rendered chat message, regardless of how many levels of indentation the source has. For deeper hierarchies, emit levels 2+ as plain text indented with **NBSPs (U+00A0)** plus a literal `- ` prefix (2 NBSPs per level). NBSP is not Markdown whitespace, so the leading `- ` is rendered verbatim rather than parsed as a nested list marker, and Teams preserves the indent visually. See `bulletPrefix(level)` in `server/queue/notificationConsumer.js` (level 1 = real Markdown bullet `- `, level 2+ = `NBSP*(level-1)*2 + '- '`). **Why:** the user directly reported "teams is only showing 1 level of bullet nesting" after a multi-level Markdown tree was deployed. **How to apply:** any new multi-level outline emitted into Teams text must follow this convention — pure-Markdown indentation will visually flatten.
- **[pattern]** When you need to verify that a source file actually contains a non-printable character like NBSP (U+00A0, bytes `c2 a0`) rather than a regular space, use `sed -n '<line>p' file | xxd | head -2`. Eyeballing the file in an editor won't tell you whether `' '` is space or NBSP. Especially important after `Edit` operations that paste NBSP-containing strings, since the harness preserves them but the bytes are not visually distinguishable.
- **[gotcha]** When changing the indentation convention used by `renderTrackable` / `bulletPrefix` / `indentAsBullet`, the existing assertions in `test/notificationConsumer.test.js` that match against `' - '` / `'  - '` literals will all silently break because the prefix bytes change. Centralise the prefixes in the test file (`const L1 = '- '; const L2 = NBSP+NBSP+'- '; ...`) and reference them from every assertion so a future indentation switch needs only one edit. Mixing literal `'  - '` (ASCII spaces) with the actual NBSP-prefixed output produces zero matches and the failure message just looks like generic "text doesn't contain expected string".
- **[pattern]** When debugging the notification consumer's render output, drive `mergeGithubTrackable` directly from a one-liner `node -e "..."` script that constructs the minimal envelope shape and chains multiple calls (each merging the previous `recent` with a new env). Faster than spinning up the full consumer + Redis, and matches exactly what the production code does. Example envelope: `{ type: 'msg', message: '...', extra: { event_type: 'check_run', _commit_sha: '<sha7>', dedup_key: 'github:commit:<sha7>', data: { check_run: { name, status, html_url } } } }`. When visualising NBSP-indented output, pipe the result through `.replace(/\u00a0/g, '·')` so the structure is visible in a terminal that otherwise renders NBSPs as plain spaces.
- **[gotcha]** `test/commands.test.js` has a long-standing failing test: `ticketQuick command "does not match unknown department"` at line 135 expects `null` but `ticketQuick.match('add foo ticket bar', ...)` returns `{ invalidDept: 'foo' }`. The other 70 tests pass and the failure pre-dates current work — don't treat it as a regression of your changes.
- **[gotcha]** Running `npm test` or `node --test 'test/*.test.js'` via the harness's Bash background runner sometimes produces zero output even after the test runner clearly started subprocesses — the runner's `--test-isolation=process` interacts badly with the harness's task-output capture. For reliable output, run a single file in the foreground with an explicit timeout: `timeout 30 node --test test/<file>.test.js`.
- **[pattern]** Every `saveRecent` call site that seeds a brand-new trackable (i.e. `handleSingleNew` and its `tryConstructedConvRef` fallback) MUST go through the `initialTrackableState(env)` helper, which runs `env.message` through `splitHeaderAndBullets` so the persisted `{ header, items, header_identity }` matches what `mergeGithubTrackable` would produce. **Why:** if you store the unsplit verbose `env.message` as `header` with `items: []`, the next re-delivery compares first-line vs multi-line, mismatches, and appends the whole event as a sub-bullet. **How to apply:** never write `header: env.message, items: []` literally — call `initialTrackableState(env)` and spread it.
- **[pattern]** The re-delivery dedup check inside `mergeGithubTrackable` compares the **first non-empty line** of the incoming `bulletMessage` against the **first non-empty line** of the stored `header`, NOT the full text. This works whether the stored header is split or in legacy unsplit form. Any rewrite of the re-delivery branch must preserve this two-sided first-line compare or existing legacy entries will start producing duplicate output.
- **[convention]** GitHub Actions check_run names use ` · ` (space-middledot-space, U+00B7) as the segment separator, not `/` or `-`. Three distinct patterns appear in practice and `decomposeCheckSegments` in `notificationConsumer.js` must handle all of them: `<Platform> · PHP X.Y · <lib>`, `<Phase> PHP X.Y · <lib>` where PHP version is glued to the leading segment (e.g. `Test PHP 8.4 · candy-vt`), and `<prefix> (<variant>)` paren style (e.g. `render (candy-vcr)`). The PHP segment is matched by `/(PHP \d+\.\d+)/`; an embedded `PHP X.Y` inside a `·`-segment is split out into its own segment.
- **[correction]** Microsoft Teams' Markdown renderer ONLY visually nests bullets one level deep — `    - foo` (4-space indent) collapses back to the same depth as `- foo`. For deeper hierarchies, emit levels 2+ as plain text indented with **NBSPs (U+00A0)** plus a literal `- ` prefix. NBSP is not Markdown whitespace, so the leading `- ` is rendered verbatim rather than parsed as a nested list marker. See `bulletPrefix(level)` in `server/queue/notificationConsumer.js`.
- **[correction]** In Teams' deep-mode NBSP-indented rendering, the lines are paragraph-continuation of the enclosing level-1 list item — Markdown joins continuation lines with a single space, so without a hard break they all collapse onto one line in Teams (`  - **Windows**   - ✅ PHP 8.3 Check success     - candy-shine (…)`). Fix: join deep-mode lines with `'  \n'` (two trailing spaces + newline) which is CommonMark's hard line break. Shallow mode keeps plain `\n` since each real Markdown list item already gets its own row natively. **Why:** the user reported nested bullets showing up all on one line after the initial NBSP indentation switch.
- **[correction]** In Teams' NBSP deep mode, an indent step of 2 NBSPs per level is too narrow to visually distinguish depth 3 from depth 4 at a glance (2 vs 4 char widths look similar). Double the per-level step to 4 NBSPs (so depth 2 = 4 NBSPs, depth 3 = 8 NBSPs). Implementation: after building lines with the original prefix, doubling the leading NBSP run on each line before joining. **Why:** the user reported "the 4th level of indent or w/e is barely noticable from the 3rd.. it needs a bit more indented". **How to apply:** keep `bulletPrefix(level)` in sync with the test constants — when changing the per-level step, update `L2_DEEP`/`L3_DEEP` in `test/notificationConsumer.test.js` in lockstep.
- **[pattern]** When you need to verify that a source file actually contains a non-printable character like NBSP (U+00A0, bytes `c2 a0`) rather than a regular space, use `sed -n '<line>p' file | xxd | head -2`. Eyeballing the file in an editor won't tell you whether `' '` is space or NBSP. Especially important after `Edit` operations that paste NBSP-containing strings.
- **[gotcha]** When changing the indentation convention used by `renderTrackable` / `bulletPrefix` / `indentAsBullet`, existing assertions in `test/notificationConsumer.test.js` that match against `' - '` / `'  - '` literals will silently break because the prefix bytes change. Centralise the prefixes in the test file (`const L1 = '- '; const L2_SHALLOW = '  - '; const L2_DEEP = NBSP.repeat(N) + '- '; ...`) and reference them from every assertion so a future indentation switch needs only one edit.
- **[pattern]** When debugging the notification consumer's render output, drive `mergeGithubTrackable` directly from a one-liner `node -e "..."` script that constructs the minimal envelope shape and chains multiple calls (each merging the previous `recent` with a new env). Faster than spinning up the full consumer + Redis. Example envelope: `{ type: 'msg', message: '...', extra: { event_type: 'check_run', _commit_sha: '<sha7>', dedup_key: 'github:commit:<sha7>', data: { check_run: { name, status, html_url } } } }`. When visualising NBSP-indented output, pipe through `.replace(new RegExp(NBSP,'g'),'▫')` so the structure is visible in a terminal that otherwise renders NBSPs as plain spaces. To see CommonMark hard breaks, also `.replace(/  \n/g, '⏎\n')`.
- **[pattern]** Teams rendering has two distinct depth regimes that need different output strategies: (1) **Shallow** (max depth 2) — use native Markdown nested bullets with ASCII-space indentation, joined with plain `\n`; Teams renders each list item on its own row natively. (2) **Deep** (depth 3+) — use NBSP-indented literal `- ` text on every level-2+ line, AND join with `'  \n'` (CommonMark hard break) so each NBSP-text line gets its own row instead of collapsing into paragraph continuation. Detect by scanning for any line starting with the depth-3 prefix; choose the rendering mode for the entire message accordingly. **How to apply:** any new Teams-bound multi-level renderer should follow this same dual-mode pattern — testing only the shallow case will let deep-mode regressions ship.
- **[gotcha]** `test/commands.test.js` has a long-standing failing test: `ticketQuick command "does not match unknown department"` at line 135 expects `null` but `ticketQuick.match('add foo ticket bar', ...)` returns `{ invalidDept: 'foo' }`. The other 70 tests pass and the failure pre-dates current work — don't treat it as a regression of your changes. Verify by stashing your changes and re-running before assuming you caused it. Fix it deliberately (either tighten `ticketQuick.match` to return null for unknown depts, or update the test) rather than as a side-effect.
- **[gotcha]** Running `npm test` or `node --test 'test/*.test.js'` via the harness's Bash background runner consistently produces zero output even when subprocesses clearly started — the runner's `--test-isolation=process` interacts badly with the harness's task-output capture and the `.output` files stay 0 bytes. This is a *recurring* trap, not a one-off — every time you reach for `run_in_background: true` for tests, expect empty output and waste cycles polling. For reliable output, run a single file in the foreground with an explicit timeout: `node --test test/<file>.test.js 2>&1 | tail -25`. To run all four test files, list them explicitly on one foreground command rather than relying on glob expansion: `node --test test/commands.test.js test/botActivityHandler.test.js test/validateEnv.test.js test/notificationConsumer.test.js`.
- **[correction]** In Teams' deep-mode NBSP-indented rendering, the lines are paragraph-continuation of the enclosing level-1 list item — Markdown joins continuation lines with a single space, so without a hard break they all collapse onto one line in Teams. Fix: join deep-mode lines with `'  \n'` (two trailing spaces + newline) which is CommonMark's hard line break. Shallow mode keeps plain `\n` since each real Markdown list item already gets its own row natively. **Why:** the user reported nested bullets showing up all on one line after the initial NBSP indentation switch.
- **[correction]** In Teams' NBSP deep mode, an indent step of 2 NBSPs per level is too narrow to visually distinguish depth 3 from depth 4 at a glance. Double the per-level step to 4 NBSPs (so depth 2 = 4 NBSPs, depth 3 = 8 NBSPs). Implementation: after building lines with the original prefix, doubling the leading NBSP run on each line before joining. **Why:** the user reported "the 4th level of indent or w/e is barely noticable from the 3rd.. it needs a bit more indented". **How to apply:** keep `bulletPrefix(level)` in sync with the test constants — when changing the per-level step, update `L2_DEEP`/`L3_DEEP` in `test/notificationConsumer.test.js` in lockstep.
- **[gotcha]** GitHub Actions emits one "skipped" check_run **per matrix cell** when a `strategy.matrix` job is gated out by a job-level `if:`, AND it does NOT substitute the `${{ matrix.* }}` templates in the check_run's `name`. The literal template text leaks downstream — e.g. webhook consumers receive a check_run literally named `Coverage · ${{ matrix.lib }}`. Detect by seeing `${{ matrix.* }}` placeholders surfacing in Teams / Slack / consumers. **Fix:** move the gate out of the job and into the matrix itself — create a `build-matrix` prereq job that emits the lib/php list as a JSON output (or `[]` when the gate is closed), then have the matrix job consume it via `matrix: ${{ fromJSON(needs.build-matrix.outputs.foo) }}`. Empty matrix → zero jobs spawn → zero skipped placeholders. See `/home/sites/sugarcraft/.github/workflows/ci.yml` commit `f813532` for the canonical example covering both single-axis (`lib`) and multi-axis (`php` + `lib`) matrices.
- **[pattern]** Action-triggered child pushes (e.g. a CI workflow that commits + pushes back to master, or a subtree-sync workflow that pushes to downstream repos) arrive as brand-new `push` events on brand-new SHAs — GitHub gives no field linking them to the workflow_run that caused them. `notificationConsumer.js` bridges that gap with two pieces: (1) `recordActiveWorkflow(env)` seeds a per-repo `notif:wfactive:{repo}` sorted set (member = parent SHA, score = ts, TTL = EDIT_WINDOW_MS) on every workflow_run/workflow_job/check_run; (2) `isActionTriggeredPush(env)` flags pushes whose pusher matches `github-actions[bot]`/`github-actions@`, whose sender ends in `[bot]`, whose head commit's author email is a github-actions identity, OR whose repo lives under a `NOTIF_DOWNSTREAM_REPOS` glob. For flagged pushes, `findParentSha(env)` looks up the most recent active parent SHA (own repo first, then any upstream from the map) and the consumer rewrites `extra._commit_sha`/`extra.dedup_key` so the push nests under the existing trackable. Default downstream map: `detain/sugarcraft:sugarcraft/*`. When adding a new bot-pusher repo, extend `NOTIF_DOWNSTREAM_REPOS` (or one of the regex tests in `isActionTriggeredPush`) rather than special-casing in the merger.
- **[convention]** When a status-bucket of leaves in `renderChecksGrouped` exceeds `LEAVES_COMPACT_THRESHOLD` (3), emit them as ONE comma-joined bullet via `leafCompact(ci)` instead of one bullet per leaf. `leafCompact` shortens `[details](url "title")` to `[d](url)` so a 30-package matrix run stays scannable. The threshold and helper live next to `leafInline`/`leafOnly` in `notificationConsumer.js` — keep new bucket emitters going through `emitLeafBucket(lines, level, leaves)` rather than open-coding the loop, otherwise the compaction won't apply to new groupings.
